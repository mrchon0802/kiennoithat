export interface SelectingOrder {
  productId: string;
  name: string;
  price: number;
  weight: number;
  height: number;
  width: number;
  length: number;
  size: string; // 👈 dùng để hiển thị
  color: string;
  quantity: number;
}
